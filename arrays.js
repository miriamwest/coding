// var shop=['poptarts', 'ramen', 'chips', 'salsa', 'coffee'];
// shop.push= ('fruitloops');
// shop[4]='fairtradecoffee';
// shop.splice (2,2,'rice', 'beans');
// var cart=[];

// var last_item=shop.pop();cart.push(last_item);
// var first_item=shop.shift();cart.push(first_item);

// while(shop.length>0)
// 	{var item=shop.pop();cart.push(item);}

// cart.sort().reverse();

// array.join(', ');'poptarts', 'ramen', 'rice', 'beans', 'fairtradecoffee', 'fruitloops';
// console.log(shopping_cart.toString());


// var course = {

// 	name: 'JavaScript Applications',

// 	awesome: true,

// 	teachers: ['Assaf', 'Shane'],

// 	students: [

// 		{name: 'Steve',

// 			computer: {

// 				OS: 'Linux',

// 				type: 'laptop'

// 			}

// 		},

// 		{name: 'Katy',

// 			computer: {

// 				OS: 'OSX',

// 				type: 'macbook'

// 			}

// 		},

// 		{name: 'Chuck',

// 			computer: {

// 				OS: 'OSX',

// 				type: 'macbook'

// 			}

// 		}

// 	],

// 	preReqs : {

// 		skills : ['html', 'css', 'git'],

// 		equipment: {

// 			laptop: true,

// 			OSOptions: ['linux', 'osx']

// 		}

// 	}

// };

// course.name=
// course.teachers[1]=
// course.students[0].name=

// course.students[1].computer.type=
// course.preReqs.equipment=

// course.preReqs.equipment.OSOptions[1];

// course.preReqs.equipment.OSOptions.join(' or ');

// var new_array = [];

// for(var i = 0; i < course.students.length; i++){
// 	if(course.students[i].computer.OS === 'OSX'){
// 		new_array.push(course.students[i]);}
// }

// console.log(new_array);



var email = {

	gmail:

			{inbox: [
				'compose',
				'read/unread',
				'drafts',
				'send',
				'junk/spam']

			trash: [
				'permanent delete']

			contacts: [
				{name: 'Palm', lastMessage: "Don't forget the fourth of July party.",
				name: 'Austin', lastMessage: "I have a monstera cutting for you."}]

			archive: [
				'save', 
				'starred', 
				'add folder']
			}

	tulane_email:[

			{inbox: 
				'compose',
				'read/unread',
				'drafts',
				'send',
				'junk/spam'}

			trash: {
				'permanent delete'
			}

			contacts: 
				{name: 'Bursar', lastMessage, "Give back today!",
				name: 'Newcomb', lastMessage, "Women's luncheon."}

			archive: {
				'save', 
				'starred', 
				'add folder'}
			]
		

	loyola_email:

			{inbox: [
				'compose',
				'read/unread',
				'drafts',
				'send',
				'junk/spam']

			trash: [
				'permanent delete']

			contacts: [
				{name: 'Callie', lastMessage, "Levelset email addresses.",
				name: 'Dean', lastMessage, "Loyno email change."}]

			archive: [
				'save', 
				'starred', 
				'add folder']
			}}
// <li>Get a list of inbox names</li>

// <li>Get a list of emails</li>

// <li>Get the text of the second email in the visible list</li>

// <li>Mark an email as sent</li>

// <li>Add a draft email to the drafts mailbox</li>

console.log course.email()
console.log 








